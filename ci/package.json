{
  "name": "ci",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild update-server/update-server.ts --outdir=dist --bundle --sourcemap --platform=node",
    "start": "node -r dotenv/config dist/update-server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.18.17"
  }
}
